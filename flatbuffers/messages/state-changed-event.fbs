include "../types/state.fbs";

namespace weidmueller.ucontrol.hub;

/// An event that describes a state that have just changed.
table StateChangedEvent {
  /// A state that have just been changed.
  state: State;
}

root_type StateChangedEvent;
