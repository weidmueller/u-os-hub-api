include "../types/provider.fbs";

namespace weidmueller.ucontrol.hub;

/// An event that describes a list of providers that have just changed.
table ProvidersChangedEvent {
  /// A list of providers that have just been changed.
  providers: ProviderList;
}

root_type ProvidersChangedEvent;
